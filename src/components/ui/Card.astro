---
interface Props { class?: string; }
const { class: className } = Astro.props;
---
<div class:list={["rounded-[var(--radius)] border bg-card text-card-foreground shadow-sm", className]}>
  <slot />
</div>